{
  "scripts": {
    "lint": "eslint --ext .js --ignore-path .gitignore .",
    "test": "NODE_ENV=development HOST=0.0.0.0 PORT=3000 jest --verbose",
    "coverage": "NODE_ENV=development HOST=0.0.0.0 PORT=3000 jest --collect-coverage"
  },
  "devDependencies": {
    "eslint": "^8.34.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-plugin-jest": "^27.2.1",
    "jest": "^29.4.3",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
